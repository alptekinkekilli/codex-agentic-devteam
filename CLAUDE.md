# CLAUDE.md

This repository is Codex-first. The primary instruction file is `AGENTS.md`.

`CLAUDE.md` is kept only as a compatibility pointer for tools or older prompts
that still look for it. Follow `AGENTS.md`, `GOVERNOR.md`, and the role charter
under `agents/` instead.
